import styled from "styled-components";

const StyledUlCart = styled.ul`
  
  margin: 0;
  padding: 18px; 
  max-height: calc(328rem/16);
  overflow-x: unset;
  overflow-y: auto;
  gap: 26px;

`

export default StyledUlCart;
